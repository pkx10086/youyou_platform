server:
  port: 8764

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: ribbon-client #ribbon不支持下划线的名字，支持横线
  zipkin:
    base-url: http://localhost:9411
